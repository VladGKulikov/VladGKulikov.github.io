# Модуль 7b. Диффузионные LLM

*Курс «Modern LLMs» · лекция модуля 7b · редакция 2026.8*

> **О чём этот модуль.** До сих пор курс менял прежде всего устройство одного блока: attention, рекуррентные механизмы SSM, MoE, нормализацию и способы хранить контекст. Здесь мы меняем другую ось — **порядок генерации**. Авторегрессионная модель (**AR — Autoregressive**) принимает уже готовый префикс и необратимо добавляет один токен. Диффузионная большая языковая модель (**dLLM — Diffusion Large Language Model**) начинает с испорченного или замаскированного черновика и несколько раз уточняет сразу множество позиций. Мы разберём поглощающее маскирование, вариационную цель **ELBO — Evidence Lower Bound**, порядок демаскирования, повторное маскирование (**remasking**) и блочную диффузию (**block diffusion**). Затем построим прозрачную учебную roofline-модель, а отдельно прочитаем реальный паспорт DiffusionGemma — так, чтобы арифметическую верхнюю границу не принять за измеренную скорость конкретной модели.
>
> **Что нужно знать заранее.** Модуль 5 — устройство Transformer-блока; модуль 6 — MoE; модуль 7 — фиксированное рекуррентное состояние и гибридные модели. Все определения, необходимые для текущего маршрута, повторяются здесь.

**Оглавление**

1. [Мотивация: изменить единицу последовательной работы](#m7b-01)
2. [Историческая линия](#m7b-02)
3. [Три интеллектуальные родословные](#m7b-03)
4. [Карта парадигм](#m7b-04)
5. [Прямой процесс: поглощающее маскирование](#m7b-05)
6. [Решённый пример A: расписание руками](#m7b-06)
7. [Цель обучения: ELBO и masked cross-entropy](#m7b-07)
8. [Any-order и границы симметрии](#m7b-08)
9. [Семплинг и решённый пример B](#m7b-09)
10. [Учебная roofline-модель: экономика одного прохода](#m7b-10)
11. [Шаги, батчи и точность весов](#m7b-11)
12. [Код первого уровня](#m7b-12)
13. [Что проверяет notebook](#m7b-13)
14. [Блочная диффузия, длина и KV-cache](#m7b-14)
15. [Паспорт DiffusionGemma](#m7b-15)
16. [Ландшафт dLLM на 6 августа 2026 года](#m7b-16)
17. [Мультимодальные системы](#m7b-17)
18. [Вопросы для самопроверки](#m7b-18)
19. [Упражнения с решениями](#m7b-19)
20. [Ключевые выводы и источники](#m7b-20)

---

<a id="m7b-01"></a>

## 1. Мотивация: изменить единицу последовательной работы

Проблема авторегрессионного декодирования не в том, что один шаг обязательно содержит много арифметики. Напротив, при пакете из одного запроса он часто выполняет слишком **мало** полезной матричной работы относительно объёма весов, который приходится прочитать. Ради одного следующего токена ускоритель вновь проводит через вычислительный тракт почти всю модель, обращается к KV-cache и лишь затем может перейти к следующей позиции.

Рассмотрим намеренно простой сценарий: плотная модель с 8 млрд параметров, bf16-веса объёмом 16 GB, пропускная способность памяти 3.35 TB/s и арифметический пик 990 TFLOP/s. Если считать только чтение весов, один AR-шаг не может быть быстрее

$$
 t_{\text{mem}}=\frac{16\cdot10^9}{3.35\cdot10^{12}}
 \approx4.776\ \text{мс}.
$$

Идеальная арифметика $2P$ заняла бы

$$
 t_{\text{compute}}
 =\frac{2\cdot8\cdot10^9}{990\cdot10^{12}}
 \approx16.16\ \text{мкс}.
$$

Отношение этих двух времён равно 0.338%. Это **не измеренная загрузка GPU**: мы всего лишь видим, какую долю выбранной нижней границы занимает идеальное время матричной арифметики. В этой упрощённой модели предел задаёт поток весов, а верхняя граница при пакете из одного запроса равна 209.375 токена/с.

Диффузионный декод меняет единицу работы. Один вызов модели обрабатывает не одну позицию, а весь холст из $N$ позиций. За это приходится платить $S$ итерациями денойзинга. Число операций становится примерно

$$
2PNS,
$$

но каждое чтение весов обслуживает множество позиций. В том же учебном сценарии при $N=1024$ один проход уже требует около 16.55 мс арифметики и становится ограниченным вычислениями (**compute-bound**). При $S=64$ арифметическая верхняя оценка составляет 966.8 токена/с — в 4.62 раза выше AR-границы при пакете из одного запроса, хотя суммарных FLOP примерно в 64 раза больше.

Эта сделка и определяет всю ветвь dLLM: **мы покупаем меньше последовательных барьеров ценой большей общей работы**. Выигрыш не гарантирован. Его могут съесть число шагов, двунаправленное attention внутри холста, маршрутизация MoE, активации, вычислительные ядра, планировщик и требования к качеству. Поэтому ниже мы сначала разберём вероятностный механизм, а уже затем вернёмся к скорости.

У dLLM есть и второй мотив. Поскольку модель несколько раз видит весь текущий черновик, она может нативно заполнять пропуски и пересматривать слабые позиции. Стандартный одношаговый AR-сэмплер уже записанный префикс не меняет; для редактирования ему нужен второй проход, верификатор, Fill-in-the-Middle (**FIM**) или агентная обвязка. В диффузионном семплере повторное уточнение является частью самой процедуры.

<a id="m7b-02"></a>

## 2. Историческая линия

![VIZ m7b/01 — развитие диффузионных языковых моделей](assets/modern-llms/ru/module-07b/m7b_01_timeline.svg)

**D3PM — Discrete Denoising Diffusion Probabilistic Models** (2021) перенесла диффузионную конструкцию в дискретные пространства. Работа рассматривала несколько переходных ядер: равномерные замены, переходы по близости в embedding-space и поглощающее состояние. Последний вариант особенно важен для текста: одна специальная маска превращается в состояние, из которого обратная модель должна восстановить токен.

**Diffusion-LM** (2022) пошла другим путём: добавляла непрерывный шум к эмбеддингам и затем отображала результат обратно в дискретные токены. Эта ветвь показала управляемость генерации, но её дополнительная геометрия округления осложняла масштабирование.

В 2024 году две линии сделали дискретную диффузию значительно практичнее. **SEDD — Score Entropy Discrete Diffusion** ввела подходящую функцию оценки для дискретного процесса. **MDLM — Masked Diffusion Language Models** и родственные работы показали, что поглощающая маска даёт простую цель, ориентированную на оценку правдоподобия и близкую к знакомому Masked Language Modeling (**MLM**), и поддерживает эффективные семплеры, включая полуавторегрессионную генерацию переменной длины.

В 2025 году вопрос сменился с «работает ли формализм?» на «может ли он стать LLM?». **LLaDA 8B** была обучена с нуля на 2.3T токенов и показала, что masked diffusion масштабируется до миллиардов параметров. **BD3-LM** предложила блочную диффузию: блоки идут авторегрессионно, а позиции внутри блока восстанавливаются параллельно. **LLaDA-MoE**, **Dream**, **Seed Diffusion**, Mercury и Gemini Diffusion исследовали разные пути — обучение с нуля, конверсию AR-checkpoint, MoE и специализированные системы инференса.

В 2025–2026 годах появились модели на десятках и сотнях миллиардов параметров. **LLaDA2.0** конвертировала AR-MoE через Warmup–Stable–Decay (**WSD**) и выпустила варианты 16B и 100B. **LLaDA2.1** добавила редактирование уже открытых токенов; речь идёт о Token-to-Token correction, а не об обязательном контракте изменения длины через `INSERT/DELETE`. **iLLaDA** усилила линию обучения с нуля: модель 8B прошла претрейн на 12T токенов, использует полностью двунаправленное внимание и поддерживает генерацию переменной длины.

Коммерческая ветвь развивалась параллельно. Mercury 2, Gemini Diffusion и Seed Diffusion публиковали очень высокие скорости, но на разном оборудовании и в разных задачах. **DiffusionGemma**, выпущенная 10 июня 2026 года, стала особенно полезным открытым примером: блочно-авторегрессионная архитектура encoder–decoder, разреженная MoE, холст на 256 токенов и подробная карточка режима семплирования.

История не ведёт к одному победившему рецепту. Она образует спектр: masked diffusion с полным холстом, блочную диффузию, редактирование токенов, вставки для изменения длины и коммерческие проприетарные системы. Чтобы сравнивать их, нам нужны две оси — вероятностная факторизация и единица последовательного прогресса.

<a id="m7b-03"></a>

## 3. Три интеллектуальные родословные

Первая родословная — непрерывная диффузия изображений. Там объект постепенно портится гауссовым шумом, а сеть учится обращать процесс. Для идентификатора токена «малое численное смещение» не имеет лингвистического смысла, поэтому дискретные модели задают явное переходное ядро.

Вторая — BERT и MLM. Двунаправленный Transformer уже умеет восстанавливать скрытый токен по левому и правому контексту. Но BERT использует один или несколько заранее заданных уровней маскирования и не определяет полноценную генеративную траекторию от полностью испорченной последовательности к чистой. Диффузионная постановка добавляет время $t$, распределение уровней порчи и обратный марковский процесс.

Третья — переменный порядок факторизации. Авторегрессионная модель выбирает один порядок:

$$
 p(x)=\prod_{i=1}^{N}p(x_i\mid x_{<i}).
$$

Masked diffusion обучается на множестве подмножеств наблюдаемых и скрытых позиций. Для некоторых целей это можно интерпретировать как усреднение множества порядков раскрытия. Но это утверждение относится к конкретной целевой функции и распределению масок; оно не делает любую dLLM архитектурно равной ансамблю всех AR-моделей.

![VIZ m7b/02 — карта парадигм](assets/modern-llms/ru/module-07b/m7b_02_axes.svg)

<a id="m7b-04"></a>

## 4. Карта парадигм

Удобно отделить способ **обучения распределения** от способа **исполнения генерации**.

| Режим | Что фиксировано | Что обновляется за один шаг | Кэширование |
| --- | --- | --- | --- |
| AR | весь префикс | один новый токен | точный KV-cache префикса |
| Полная диффузия с маскированием | условие/промпт | весь холст | меняющийся холст пересчитывается |
| Блочная диффузия | завершённые блоки | текущий блок | завершённый префикс кэшируется |
| Редактирование токенов | часть черновика | выбранные позиции | зависит от стабильности сегментов |
| Гибкая диффузия с маскированием | текущая последовательность | маски и вставки | зависит от конкретного алгоритма |

Эта таблица сразу убирает несколько ложных противопоставлений. Диффузионная модель не обязана иметь фиксированную длину: полуавторегрессионная MDLM, блочная диффузия, FlexMDM и iLLaDA предлагают разные решения. И наоборот, блочная диффузия не становится «почти обычной AR»: внутри блока остаются двунаправленное обусловливание и параллельное раскрытие.

<a id="m7b-05"></a>

## 5. Прямой процесс: поглощающее маскирование

В этом модуле основным примером служит **absorbing masked diffusion** — диффузия с поглощающей маской. Это одна важная, но не единственная форма дискретной диффузии. D3PM допускает и другие переходные матрицы, включая случайные замены токенов.

Пусть чистая последовательность равна $x_0=(x_0^1,\ldots,x_0^N)$. При линейном расписании в момент $t\in[0,1]$ каждая разрешённая позиция независимо сохраняется с вероятностью $1-t$ и заменяется специальным токеном $m$ с вероятностью $t$:

$$
q(x_t^i\mid x_0^i)
=
(1-t)\,\delta_{x_0^i}(x_t^i)
+t\,\delta_m(x_t^i).
$$

Слово «поглощающее» означает, что в прямом процессе маска не превращается обратно в содержательный токен. Обратная модель решает противоположную задачу: по частично наблюдаемой последовательности предсказывает исходные значения скрытых позиций.

При условной генерации промпт можно защитить от порчи: маскирование действует только на область ответа. Это важно при работе с инструментами и длинным контекстом: условие остаётся фиксированным, а генерируемая часть итеративно уточняется.

![VIZ m7b/03 — прямой процесс](assets/modern-llms/ru/module-07b/m7b_03_forward.svg)

<a id="m7b-06"></a>

## 6. Решённый пример A: расписание руками

Возьмём восемь позиций и четыре модельных вызова. Полностью замаскированная последовательность должна пройти через целевые количества масок

$$
[8,6,4,2,0].
$$

Удобная явная политика:

$$
m_s=\left\lceil N\frac{S-s}{S}\right\rceil,
\qquad s=0,\ldots,S.
$$

На каждом шаге открываются ровно две позиции. Если $S>N$, некоторые соседние значения совпадут: вызов модели может не открыть ни одной новой позиции. Это не математическая ошибка, но такое расписание тратит вычисления без изменения числа масок.

Для непрерывного линейного процесса в моменты $t=0.25,0.5,0.75$ вес среза равен соответственно

$$
\frac1t=4,
\qquad2,
\qquad\frac43.
$$

При малом $t$ скрыто мало позиций, поэтому каждая из них получает больший вес. На уровне мини-батча это может повышать дисперсию; практические рецепты используют дискретизацию времени, выборку по важности или другие расписания шума.

<a id="m7b-07"></a>

## 7. Цель обучения: ELBO и masked cross-entropy

Для линейного absorbing process непрерывновременная вариационная цель упрощается до взвешенной cross-entropy на замаскированных позициях. В конвенции этого модуля для одного среза:

$$
L_t
=
\frac1t
\sum_{i\in M_t}
-\log p_\theta(x_0^i\mid x_t,t),
$$

где $M_t$ — множество испорченных позиций. Мы используем **сумму**, а не среднее по маскам. Если другой trainer усредняет по числу скрытых токенов, численное значение будет иным даже при тех же logits.

Пусть правильные токены получают вероятности

$$
[0.9,0.7,0.6,0.8]
$$

и все четыре позиции скрыты при $t=0.5$. Тогда

$$
L_{0.5}
=
2\left[-\log0.9-\log0.7-\log0.6-\log0.8\right]
=2.392009269.
$$

![VIZ m7b/04 — цель masked diffusion](assets/modern-llms/ru/module-07b/m7b_04_loss.svg)

В коде $t=0$ нужно обрабатывать отдельно. В непрерывном распределении одна точка имеет нулевую меру, но вычисление $1/t$ всё равно должно явно отклонять ноль.

Здесь полезно аккуратно говорить о правдоподобии (**likelihood**). AR-модель имеет точную левостороннюю факторизацию, поэтому её **NLL — Negative Log-Likelihood** вычисляется непосредственно. Многие dLLM оптимизируют ELBO или оценивают **NELBO — Negative Evidence Lower Bound**, но существуют формулировки masked diffusion, ориентированные на оценку правдоподобия, включая MDLM. Поэтому perplexity для dLLM нельзя объявить бессмысленной; нужно фиксировать способ оценки, число временных срезов и общий протокол.

<a id="m7b-08"></a>

## 8. Any-order и границы симметрии

Случайные маски заставляют сеть решать множество условных задач на одном документе. В одном мини-батче скрыто начало сущности, в другом — её конец, в третьем — отношение между двумя уже наблюдаемыми фрагментами. Это даёт более симметричный обучающий сигнал, чем единственная левая факторизация.

Для целей с равномерным распределением порядков masked diffusion можно связать с авторегрессией, усреднённой по порядкам. Но не все порядки одинаково информативны для естественного языка: работа Any-Order GPT показывает, что естественная структура языка слева направо остаётся полезным индуктивным смещением. Здесь нет противоречия. Диффузия расширяет множество условных задач, а не доказывает бесполезность последовательного порядка.

То же ограничение относится к reversal curse. LLaDA сообщала сильный результат на reversal poem completion, однако более поздняя работа DiffER обнаружила reversal curse и у dLLM. Причины включают фрагментацию сущностей, асимметрию данных и отсутствие обратных отношений. Bidirectional masking может смягчать позиционную асимметрию, но не создаёт отсутствующий факт автоматически.

Наконец, при ограниченном корпусе случайные маски действительно могут работать как неявное расширение задач. Работа *Diffusion Beats Autoregressive in Data-Constrained Settings* обнаружила преимущество masked diffusion при многократном использовании конечного набора данных. Это эмпирический результат выбранных масштабов и режимов повторения, а не универсальное доказательство большей эффективности использования данных.

<a id="m7b-09"></a>

## 9. Семплинг и решённый пример B

Обратный процесс требует не только logits, но и политики выбора позиций. Простейший sampler на каждом шаге:

1. предсказывает распределение для всего холста;
2. выбирает кандидата на токен в каждой маске;
3. ранжирует позиции;
4. открывает столько позиций, сколько требует следующий mask count;
5. при необходимости возвращает слабые открытые позиции в маску.

Распространённые критерии:

- случайный порядок;
- максимальная вероятность выбранного токена;
- минимальная энтропия;
- обучаемый порядок;
- remasking низкоуверенных позиций;
- расписания, зависящие от блока или задачи.

В toy trace модуля восемь позиций открываются группами по две. Confidence policy сначала выбирает наиболее уверенные anchors. Итог совпадает с шестью из восьми target tokens. Это не сравнительное измерение: logits намеренно сконструированы, чтобы показать влияние порядка.

![VIZ m7b/05 — трасса демаскирования](assets/modern-llms/ru/module-07b/m7b_05_demask_trace.svg)

Remasking даёт слабой позиции второй шанс после того, как вокруг появилось больше контекста. В стандартном AR sampling аналогичный эффект требует внешней процедуры revision; dLLM включает его в sampler. Но даже здесь self-correction не бесплатна: повторно замаскированный токен увеличивает число полезных итераций.

DiffusionGemma рекомендует более сложный семплер **EB — Entropy Bound**. Он выбирает низкоэнтропийные токены при ограничении mutual-information bound, полностью renoise остальные позиции и может остановиться адаптивно, когда entropy мала, а наиболее вероятный токен стабилен два шага подряд. Это хороший пример того, что sampler является частью модели-паспорта, а не второстепенным флагом.

<a id="m7b-10"></a>

## 10. Учебная roofline-модель: экономика одного прохода

Теперь вернёмся к сценарию плотной 8B-модели из §1. Подчеркнём его статус: это **не DiffusionGemma**, не MoE и не измерение H100. Модель изолирует только два нижних предела — поток весов и идеальную матричную арифметику.

Для полного холста длины 1024 один проход требует

$$
2PN
=
2\cdot8\cdot10^9\cdot1024
=16.384\ \text{TFLOP}.
$$

При 990 TFLOP/s:

$$
t_{\text{compute}}=16.549\ \text{мс}.
$$

Чтение 16 GB весов остаётся 4.776 мс, поэтому проход становится ограниченным вычислениями. С $S$ итерациями:

$$
Q_{\text{dLLM}}
\le
\frac{1024}{S\cdot16.549\ \text{мс}}.
$$

Отсюда:

| Шаги $S$ | Арифметическая верхняя оценка |
| --- | --- |
| 16 | 3867.2 ток/с |
| 64 | 966.8 ток/с |
| 128 | 483.4 ток/с |

Паритет с AR-границей при пакете из одного запроса находится около

$$
S^*\approx295.5.
$$

На целых шагах 295 ещё немного быстрее, 296 — уже нет. Это не рекомендация использовать 295 итераций. Формула лишь показывает, насколько дорогим может стать параллельное денойзинг-уточнение до потери арифметического преимущества в заданной абстракции.

![VIZ m7b/06 — roofline учебного сценария](assets/modern-llms/ru/module-07b/m7b_06_roofline.png)

<a id="m7b-11"></a>

## 11. Шаги, батчи и точность весов

Увеличение размера пакета помогает и AR: одно чтение весов обслуживает несколько последовательностей. Если грубо предположить линейный рост пропускной способности AR, dLLM с $S=64$ сравнивается с ним при

$$
B^*\approx\frac{966.8}{209.4}=4.62.
$$

Эта точка не универсальна. При нескольких одновременных запросах добавляются KV-трафик, разные длины, накладные расходы планировщика и эффекты насыщения вычислительных ядер. Но вывод важен: преимущество параллельного демаскирования особенно естественно для интерактивного инференса с малым пакетом, а не автоматически для сервера с уже насыщенной пакетной обработкой.

Рассмотрим отдельно блок 256 в той же **плотной 8B-модели**. Арифметика прохода занимает 4.137 мс. При bf16 чтение 16 GB занимает 4.776 мс, поэтому нижнюю границу задаёт память. Если идеализированно считать весь банк четырёхбитным, объём весов равен 4 GB, чтение — 1.194 мс, и нижняя граница переходит к вычислениям. При 16 шагах получается 3867.2 токена/с.

![VIZ m7b/07 — цена числа итераций](assets/modern-llms/ru/module-07b/m7b_07_tps.png)

Сравнение показывает роль численной точности, но не позволяет просто взять число 3867 и приписать его реальной MoE-модели. В разреженной MoE разные позиции одного холста могут маршрутизироваться к разным экспертам; весовой трафик определяется объединением затронутых экспертов, форматом тензоров и средой исполнения. Именно поэтому в следующем разделе с паспортом мы не будем выводить скорость DiffusionGemma из 3.8B активных параметров одной формулой.

<a id="m7b-12"></a>

## 12. Код первого уровня

Два базовых контракта: loss видит только испорченные позиции, а sampler вызывает модель один раз на итерацию и открывает строго запланированное число токенов.

```python
import math
import numpy as np

def linear_mask_schedule(length: int, steps: int) -> tuple[int, ...]:
    """Target mask counts: fully masked → fully revealed."""
    if length <= 0 or steps <= 0:
        raise ValueError("length and steps must be positive")
    return tuple(
        math.ceil(length * remaining / steps)
        for remaining in range(steps, -1, -1)
    )

def dllm_loss(logits, target_ids, loss_mask, time: float) -> float:
    """(1/t) · sum(-log p) under the explicit sum-reduction convention."""
    logits = np.asarray(logits, dtype=np.float64)
    target_ids = np.asarray(target_ids)
    loss_mask = np.asarray(loss_mask)
    if logits.ndim != 2 or target_ids.shape != (logits.shape[0],):
        raise ValueError("expected logits [T,V] and target_ids [T]")
    if loss_mask.shape != target_ids.shape or loss_mask.dtype.kind != "b":
        raise ValueError("loss_mask must be boolean [T]")
    if not (0.0 < time <= 1.0) or not loss_mask.any():
        raise ValueError("time must be in (0,1] and loss_mask must be non-empty")
    if not np.isfinite(logits).all():
        raise ValueError("logits must be finite")
    shifted = logits - logits.max(axis=-1, keepdims=True)
    log_probs = shifted - np.log(np.exp(shifted).sum(axis=-1, keepdims=True))
    nll = -log_probs[np.arange(target_ids.size), target_ids]
    return float(nll[loss_mask].sum() / time)

def sample_dllm(predict, length: int, steps: int, mask_id: int):
    """Greedy confidence demasking with one model call per step."""
    schedule = linear_mask_schedule(length, steps)
    tokens = np.full(length, mask_id, dtype=np.int64)
    trace = []
    for step, target_masked in enumerate(schedule[1:], start=1):
        logits = np.asarray(predict(tokens.copy(), step), dtype=np.float64)
        if logits.ndim != 2 or logits.shape[0] != length or not np.isfinite(logits).all():
            raise ValueError("predict must return finite logits [T,V]")
        if 0 <= mask_id < logits.shape[1]:
            raise ValueError("mask_id must lie outside the vocabulary")
        shifted = logits - logits.max(axis=-1, keepdims=True)
        probs = np.exp(shifted)
        probs /= probs.sum(axis=-1, keepdims=True)
        candidates = probs.argmax(axis=-1)
        confidence = probs[np.arange(length), candidates]
        masked = np.flatnonzero(tokens == mask_id)
        open_count = masked.size - target_masked
        order = np.lexsort((masked, -confidence[masked]))
        opened = masked[order[:open_count]]
        tokens[opened] = candidates[opened]
        trace.append((tuple(opened), tokens.copy()))
    return tokens, trace
```

В промышленном семплере появятся категориальная выборка, повторное маскирование, критерии на основе энтропии, адаптивная остановка, защищённые области промпта и объединённые вычислительные ядра. Но три инварианта останутся: идентификатор маски не смешивается со словарём, способ редукции функции потерь задан явно, а число масок после каждого шага соответствует расписанию.

<a id="m7b-13"></a>

## 14. Блочная диффузия, длина и KV-cache

Полный bidirectional canvas меняется на каждой итерации. Если пересчитать один токен, его новое представление может изменить все остальные позиции; обычный autoregressive KV-cache внутри холста неприменим.

Block diffusion проводит границу. Пусть завершённый префикс уже зафиксирован, а текущий блок имеет длину $L_b$. Префикс остаётся causal и кэшируется, текущий блок читает весь префикс и себя bidirectionally. После завершения блок присоединяется к префиксу, и начинается следующий.

![VIZ m7b/08 — спектр block diffusion](assets/modern-llms/ru/module-07b/m7b_08_block_spectrum.svg)

Пределы понятны:

- $L_b=1$ даёт AR-подобный порядок;
- большой $L_b$ увеличивает параллелизм, но делает каждый проход дороже;
- завершённый префикс кэшируется;
- текущий блок пересчитывается;
- последовательность может расти блок за блоком.

BD3-LM формализует этот промежуточный класс и поддерживает генерацию произвольной длины. MDLM также описывает полуавторегрессионное семплирование переменной длины, а FlexMDM вводит вставки масок. Следовательно, блочная диффузия — важный, но не единственный ответ на ограничение фиксированного холста.

LLaDA2.0 использует размер блока ещё и как учебную траекторию преобразования авторегрессионного checkpoint. В warmup block size растёт

$$
1\rightarrow4\rightarrow32\rightarrow64\rightarrow4096,
$$

стабильная фаза работает с маской всей последовательности, а фаза спада возвращает компактный размер блока. LLaDA2.1 добавляет Token-to-Token editing: открытый токен можно заменить, если последующая итерация считает его слабым. Это пересмотр значений, а не обязательное изменение длины через `INSERT/DELETE`.

<a id="m7b-15"></a>

## 15. Паспорт DiffusionGemma

Теперь применим дисциплину паспорта к реальной модели, не подменяя её сценарием плотной 8B-модели.

![VIZ m7b/09 — паспорт DiffusionGemma](assets/modern-llms/ru/module-07b/m7b_09_gemma_passport.svg)

| Поле | Опубликованный контракт |
| --- | --- |
| Архитектура | encoder–decoder, блочно-авторегрессионная диффузия по нескольким холстам |
| Total / active parameters | 25.2B / 3.8B |
| Слои | 30 |
| Эксперты | 128 routed, top-8, плюс один shared |
| Холст | 256 токенов |
| Контекст | до 256K |
| Локальное окно | 1024 |
| Вход | текст, изображения и видео как последовательности кадров |
| Выход | текст |
| Семплер | Entropy-Bounded Denoising, адаптивная остановка, до 48 шагов |
| Прогресс одного прохода | обычно 15–20 окончательно зафиксированных токенов |
| Опубликованная скорость | более 1100 ток/с, H100, FP8, малый пакет |
| Нативный низкобитный путь | NVFP4 на NVIDIA Blackwell |

Промпт обрабатывается авторегрессионным encoder и превращается в кэшируемый контекст. Decoder итеративно уточняет двунаправленный холст через cross-attention к этому префиксу. После завершения холст проходит через encoder и добавляется к кэшу. Поэтому модель одновременно использует блочную диффузию и кэш завершённой истории.

Четырёхбитная арифметика даёт две полезные, но неполные границы:

$$
25.2\text{B}\times\frac4{8}=12.6\text{ GB}=11.735\text{ GiB},
$$

$$
3.8\text{B}\times\frac4{8}=1.9\text{ GB}=1.770\text{ GiB}.
$$

Первая — полный банк, вторая — число активных параметров на токен. Ни одна не равна трафику одного вызова модели: 256 позиций могут выбрать разные наборы экспертов, а реальная среда исполнения использует неоднородные форматы, метаданные, кэш и специализированные вычислительные ядра.

Опубликованные результаты также показывают цену скорости: на многих сравнительных тестах DiffusionGemma уступает AR-Gemma 4 того же семейства, хотя на отдельных задачах ведёт себя конкурентно. Поэтому корректный продуктовый вопрос звучит не «быстрее ли diffusion?», а «какое соотношение качества и задержки даёт конкретный семплер на моей задаче?»

<a id="m7b-16"></a>

## 16. Ландшафт dLLM на 6 августа 2026 года

Скорости ниже нельзя сортировать одной колонкой: различаются оборудование, численная точность, нагрузка, размер пакета, семплер и определение выходного токена.

| Система | Ветка и доступ | Опубликованный сигнал | Как читать |
|---|---|---|---|
| LLaDA 8B | открытые веса, обучение с нуля | 2.3T токенов претрейна | свидетельство масштабируемости masked diffusion; результат reversal-задачи — отдельное измерение |
| LLaDA-MoE 7B-A1B | открытые веса | разреженная MoE-диффузия, 1.4B активных параметров | число активных параметров не определяет системную скорость без паспорта среды исполнения |
| LLaDA2.0 16B / 100B | открытые веса, конверсия AR | WSD и блочная диффузия | скорость зависит от CAP/dInfer, оборудования и порога фиксации токенов |
| LLaDA2.1 | открытые веса | Token-to-Token editing | пересмотр открытых токенов не равен вставкам для изменения длины |
| iLLaDA 8B | открытые веса, обучение с нуля | 12T токенов претрейна, переменная длина | сильная ветвь обучения с нуля; универсальный множитель TPS не заявляется |
| Mercury 2 | коммерческий API | 1009 ток/с на Blackwell, 128K | результат разработчика для конкретной продуктовой конфигурации |
| Gemini Diffusion | экспериментальная демонстрация | 1479 ток/с семплирования, накладные расходы 0.84 с | опубликованная скорость явно не включает фиксированную задержку |
| Seed Diffusion Preview | эксперимент, фокус на коде | 2146 ток/с на H20 | результат разработчика на задачах программирования |
| DiffusionGemma | открытые веса | >1100 ток/с, H100 FP8; 15–20 токенов за проход | наиболее полный открытый системный паспорт |

Mercury 2 особенно полезна как коммерческий пример dLLM с рассуждением с использованием инструментов и JSON по заданной схеме. Gemini Diffusion демонстрирует, почему скорость семплирования нужно отделять от полной задержки: фиксированные 0.84 секунды не входят в 1479 ток/с. Seed Diffusion показывает другую границу — специализированную модель для программирования на H20.

iLLaDA важна по другой причине. Она не гонится за одной цифрой TPS, а показывает, что полностью двунаправленную цель с маскированием можно масштабировать до 12T токенов претрейна и сохранить в SFT. Это напоминание: поле развивается одновременно по трём осям — качество базовой модели, гибкость семплера и системная реализация.

<a id="m7b-17"></a>

## 17. Мультимодальные системы

Слово «diffusion» в мультимодальной модели может обозначать разные процессы.

**LLaDA-V** использует masked diffusion для визуально-языкового понимания: изображение кодируется, а текстовый ответ восстанавливается диффузионно.

**LLaDA2.0-Uni** идёт дальше. SigLIP-VQ переводит визуальные входы в дискретные семантические токены; основная MoE-dLLM работает с текстовыми и визуальными токенами, а отдельный диффузионный decoder восстанавливает пиксели. Диффузия текстовых токенов и диффузия в декодере изображений — разные слои системы.

**DiffusionGemma** принимает текст, изображения и видео в одном контексте, но генерирует текст. Визуальный encoder не означает, что пиксели восстанавливаются тем же masked-token процессом, которым создаётся ответ.

Полезный вопрос для любого мультимодального паспорта:

> где именно находится diffusion — в токенах языка, в визуальном латентном decoder, в общей дискретной основе или сразу в нескольких компонентах?

Без этого две «мультимодальные диффузионные модели» могут иметь почти несопоставимые вычислительные контракты.

<a id="m7b-18"></a>

## 20. Ключевые выводы и источники

![VIZ m7b/10 — диффузионные LLM в одной схеме](assets/modern-llms/ru/module-07b/m7b_10_cheatsheet.svg)

**Парадигма.** AR необратимо добавляет токен к префиксу. Masked diffusion итеративно уточняет множество позиций. Блочная диффузия сочетает каузальный префикс и двунаправленный текущий блок.

**Прямой процесс.** В выбранной постановке с поглощающей маской позиция маскируется с вероятностью $t$. Это не единственное дискретное диффузионное ядро.

**Функция потерь.** Для линейного процесса используется взвешенная cross-entropy по маскам. Число 2.392009269 относится к суммированию в конкретном примере.

**Семплирование.** Порядок раскрытия, энтропия/уверенность, повторное маскирование и адаптивная остановка являются частью алгоритма. Один и тот же checkpoint может давать разные соотношения качества и задержки при разных семплерах.

**Roofline.** Расчёт плотной 8B-модели на H100 — учебная нижняя граница, не бенчмарк DiffusionGemma. 0.338% — доля идеального арифметического времени, а не измеренная загрузка GPU.

**DiffusionGemma.** 25.2B total / 3.8B active parameters, 128 routed experts, top-8 и один shared expert, холст 256, 15–20 финализируемых токенов за проход, >1100 ток/с на H100 FP8; NVFP4 — нативный путь для Blackwell. Идеализированные четырёхбитные объёмы весов не равны трафику одного прохода.

**Ландшафт.** Mercury 2, Gemini Diffusion, Seed Diffusion и DiffusionGemma публикуют скорости в разных протоколах. Таблица без оборудования, численной точности, семплера и накладных расходов вводит в заблуждение.

**Ключевые первоисточники:**

- [D3PM](https://arxiv.org/abs/2107.03006)
- [Diffusion-LM](https://arxiv.org/abs/2205.14217)
- [SEDD](https://arxiv.org/abs/2310.16834)
- [MDLM](https://arxiv.org/abs/2406.07524)
- [LLaDA](https://arxiv.org/abs/2502.09992)
- [BD3-LM](https://arxiv.org/abs/2503.09573)
- [Any-Order GPT as Masked Diffusion](https://arxiv.org/abs/2506.19935)
- [Data-Constrained Diffusion](https://arxiv.org/abs/2507.15857)
- [LLaDA2.0](https://arxiv.org/abs/2512.15745)
- [DiffER](https://arxiv.org/abs/2601.07347)
- [LLaDA2.1](https://arxiv.org/abs/2602.08676)
- [LLaDA2.0-Uni](https://arxiv.org/abs/2604.20796)
- [iLLaDA](https://arxiv.org/abs/2606.25331)
- [DiffusionGemma model card](https://huggingface.co/google/diffusiongemma-26B-A4B-it)
- [DiffusionGemma announcement](https://deepmind.google/models/gemma/diffusiongemma/)
- [Mercury 2](https://www.inceptionlabs.ai/blog/introducing-mercury-2)
- [Gemini Diffusion](https://deepmind.google/models/gemini-diffusion/)
- [Seed Diffusion](https://seed.bytedance.com/en/public_papers/seed-diffusion-a-large-scale-diffusion-language-model-with-high-speed-inference)

**Дальше:** модуль 8 возвращается к pretraining. Там мы обсудим, как бюджет данных, повторные эпохи и целевая функция влияют на итоговую модель; эмпирический аргумент об ограниченных данных этого модуля станет одним из примеров.

---

*Ландшафт сверен: 6 августа 2026 года. Скорости Mercury, Gemini, Seed и DiffusionGemma являются результатами разработчиков в разных аппаратных протоколах и режимах семплирования. Числа §§10–11 принадлежат отдельному учебному сценарию плотной 8B-модели.*
